# API base URL is read from app.json's `expo.extra.apiBaseUrl`, not from here
# — see src/lib/config.ts. It defaults to localhost when unset.

# Mapbox — get both from https://account.mapbox.com/access-tokens/.
# Public token: default public scopes. Download token: needs the
# DOWNLOADS:READ secret scope, used only at build time for the SDK itself.
EXPO_PUBLIC_MAPBOX_PK=
RNMAPBOX_MAPS_DOWNLOAD_TOKEN=

# Local release signing — see plugins/withReleaseSigning.js.
# Filled in automatically by `bun scripts/setup-android-keystores.ts`.
# Absent entirely, release builds fall back to debug signing (same as stock).
RELEASE_STORE_FILE=
RELEASE_STORE_PASSWORD=
RELEASE_KEY_ALIAS=
RELEASE_KEY_PASSWORD=
